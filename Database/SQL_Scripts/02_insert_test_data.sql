-- INSERT TEST DATA

-- UrgencyType
INSERT INTO
  UrgencyType (id, name, description)
VALUES
  (10, 'low', 'The value is low'),
  (20, 'medium', 'The value is medium'),
  (30, 'high', 'The value is high');

-- EventType

-- LocationType

-- Area

-- Location

-- User

-- AreaUser     (NOT area_user anymore!)

-- Event
